# Taproot channels profile - enable simple taproot channels
# Use for testing taproot channel functionality
# Note: overlay channels require special build, so only simple-taproot-chans is enabled

LND_DEBUG=debug
LND_EXTRA_ARGS="--protocol.simple-taproot-chans"
